import React, {FC, ReactElement} from 'react';

// React Native
import {SafeAreaView, Text} from 'react-native';

const MatchersScreen: FC<{}> = (): ReactElement => (
  <SafeAreaView>
    <Text>Matchers Screen</Text>
  </SafeAreaView>
);

export default MatchersScreen;
